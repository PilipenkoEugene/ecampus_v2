import 'package:flutter/material.dart';

const kPrimaryColor = Color.fromRGBO(18, 18, 18, 1);
const kPrimaryLightColor = Color.fromARGB(255, 100, 100, 100);
const kSecondaryColor = Color.fromRGBO(17, 148, 200, 1);
const kTextColor = Colors.white;

const kAnimationDuration = Duration(milliseconds: 200);

bool kSplashScreenEnable = true;
